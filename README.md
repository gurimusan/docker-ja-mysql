MySQL Locale with ja in Docker image
====================================

This docker iamge run newvim inside a Docker container.

Run, and up following.

    $ docker run -it --rm gurimusan/ja-mysql mysql -uroot -p
